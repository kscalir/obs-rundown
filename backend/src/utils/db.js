const sqlite3 = require('sqlite3').verbose();const path = require('path');const dbPath = path.join(__dirname, '../../database.sqlite');const getDb = () => new sqlite3.Database(dbPath);const handleError = (res, err, operation) => {  console.error(`DB ${operation} error:`, err);  return res.status(500).json({ error: `Database ${operation} failed` });};module.exports = {  getDb,  handleError,  dbPath};